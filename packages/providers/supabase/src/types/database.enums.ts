/**
 * Database Enums
 *
 * Define your database enums here.
 * These should match the enums defined in your Supabase database.
 *
 * Example:
 * export enum UserRole {
 *   ADMIN = 'admin',
 *   USER = 'user',
 *   GUEST = 'guest',
 * }
 */

// Placeholder enum - add your actual enums as needed
export enum PlaceholderStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
}

